# 複数エージェント開発システム

このファイルは、このプロジェクトで Codex / MCP / サブエージェントを使って開発作業を行うときの共通ルールをまとめる。  
目的は、調査・実装・検証・記録を分離し、最小の変更で根本原因を解決すること。

## 1. 基本方針

- 回答、見出し、説明、調査メモは原則として日本語で書く。ツール名、コマンド名、公式名称、コード上の識別子は英語のままでよい。
- 品質を最優先する。必要な調査・記録・相談を省略しない。
- サブエージェントを使える依頼では、調査・実装・検証を分割し、できる限り並行で進める。
- 対話的プロンプトを避け、非対話オプションを使う。
- 依頼達成に必要な情報が不足している場合は、必要な情報を具体的に依頼主へ確認する。

## 2. 実装原則

### 2.1 標準準拠・最小実装

- 公式推奨、標準準拠、モダンな実装を優先する。
- 余計な抽象を持ち込まず、根本問題を少ないコードで解決する。
- コードを修正・追加すること自体を目的にしない。
- データ加工は極力避け、取得データをそのまま活用する。加工が必要な場合は、共通化の可能性を先に調査する。
- 後方互換性は原則不要。現行要件に対する正しい実装を優先する。

### 2.2 正とするデータ・抽象

- 正とするデータは1つに固定する。候補が複数ある場合は、ログ調査を経て特定する。
- ロジック作成時は、何を正とするデータとして扱うかを記録する。
- 誤った抽象、歴史的負債、暫定対応は延命しない。必要なら削除し、正しい抽象を再導入する。
- フォールバックで問題を隠さない。失敗は即エラーとして顕在化させる。
- 自動復旧や「なあなあの継続」を避ける。

### 2.3 ログ・可観測性

- UI を含むすべての動作確認を、ログだけでリプレイ可能な情報量で記録する。
- ログは JSON など構造化形式を優先する。
- 入力、処理、出力、副作用を追跡できるようにする。
- 時刻と所要時間は必須で記録する。
- ログ水準は `INFO`、`DEBUG` / `STATES`、`SUCCESS`、`WARNING`、`ERROR` を使い分ける。

## 3. 作業開始時の確認

1. プロジェクト構造を確認する。第一候補は `tree`、使えない場合は対象を絞った `find` / `ls -R` とする。
2. 過去の Codex セッションに似た議論・失敗・実装方針がありそうな場合は、短い固有語でセッションやメモリを検索する。
3. `_docs` 配下を確認し、過去の情報収集・実装・判断を把握する。
4. 参照や編集に入る前に、使う道具を決める。広い出力を読んでから絞るのではなく、まず対象を絞ってから読む。

## 4. 道具の使い分け

- 単純 grep / 完全一致検索は `rg` を第一候補にする。
- 構文形状検索は `ast-grep` を使う。
- ライブラリ仕様は一次情報、Context7、DeepWiki、`coding-confidant` の順で確認する。
- 通常編集は Codex 純正 `apply_patch` を使う。
- 大きな出力は必要部分だけを保存・参照し、不要な全文出力を会話へ流さない。

## 5. ライブラリ・外部情報の調査

- ライブラリ仕様は一次情報を確認する。
- GitHub 上の関連パッケージ / ライブラリは DeepWiki を優先する。
- その他の関連パッケージ / ライブラリは Context7 を優先する。
- 最新動向、コミュニティ反応、X 上の採用感が判断に効く場合は `trend-researcher` も使う。
- パッケージやライブラリは最新利用を基本にする。

## 6. 外部 LLM / MCP 送信時のルール

- 外部 LLM / MCP 送信を伴う調査・相談は、「ローカルで読んだだけ」と見なさない。
- 送信したベースプロンプト、task、repomix 出力、含めた `_docs`、ログ、設定、補助ファイルを明示する。
- 認証情報、トークン、秘密鍵、ローカル状態、キャッシュ、`.venv`、明らかな一時物は原則送らない。
- 送信後のリクエスト / レスポンスは `_docs/_skills/` 配下を正本として保存する。

## 7. ファイル操作・編集

- 破壊的操作（削除 / 置換 / 権限変更など）は常に明示承認を得る。
- ワイルドカード削除（`rm -rf *` など）は禁止する。
- 削除が必要な場合は、直接削除せず `_trash` フォルダへ移動する。ただし依頼主が容量確保のため完全削除を明示した場合は、対象を列挙して承認を得た上で削除する。
- ファイル削除は参照箇所を影響調査した上で行い、影響範囲を要約する。
- 単一ファイルの編集または作成は、必ず `apply_patch` を使う。
- 失敗時の3回以上の自動リトライは禁止する。
- 適用後は静的解析 / 型チェックを優先実行し、エラーゼロを確認してから次の編集に進む。

## 8. 実装設計・実装・検証

### 8.1 実装設計

- 実装前に、必要に応じて `coding-confidant` へ、ライブラリ仕様を含めた当プロジェクトの現状、依頼内容、実装方向性を渡して調査する。
- 最新動向が効く場合は `trend-researcher` も並行で使う。

### 8.2 実装

- 依頼を遂行する最もモダンで公式推奨な、シンプルかつ根本的なロジックで実装する。
- 修正依頼時は、ログと実装から原因を列挙し、原因特定用ログを増設し、選択肢を検証する。
- ブラウザ確認可能な場合は、コンソール、ネットワーク、トレースを確認する。
- 必要な判断経緯は、コード内コメントまたは `_docs` に記録する。

### 8.3 定期リファクタリング

- 実装が複数ターン・複数機能にまたがった場合は、機能追加だけで完了とせず、プロジェクト全体を見直すリファクタリング工程を設ける。
- リファクタリングでは、ファイル名、フォルダ構成、責務境界、起動経路、設定名、環境変数、ログ名、ドキュメント名が現在の正しい抽象に揃っているかを確認する。
- 追加実装のたびに生まれた暫定ファイル、古い実験スクリプト、重複した起動コマンド、使われなくなった設定、古いモデル名、不要な fallback を洗い出す。
- 削除・改名・移動は参照元を `rg` で調べ、影響範囲と判断理由を `_docs` に残す。
- リファクタリング後は、変更内容に応じて最低限の機械的確認を実行する。例: 構文検査、該当ユニットテスト、設定検証コマンド、readiness / health endpoint、最小 smoke request、主要 CLI の dry-run。
- 公開 / 配布用ディレクトリや別リポジトリが存在する場合は、リファクタリング後の構成・README・サンプル env・テストを同期し、差分の意味がある場合だけ commit / push する。

## 9. ドキュメント・課題管理

- 収集した情報は `_docs` 配下に分かりやすいファイル名の Markdown として記録する。
- 古い記述が残る場合は、追記だけでなく最新の正本へ整理する。
- 気づいた課題は `_docs/issue.md` に記載する。
- 不要要素、失敗の不可視化、自動復旧、不要な後方互換性は削除する。

## 10. 公開・配布

- 公開用 Git はホワイトリスト運用を原則とし、自作の設定・自作スキルだけを公開対象にする。
- API キーやトークンは記載せず、環境変数参照へ寄せる。
- 公開済みの汎用スキルは再配布せず、README で配布元を案内する。

## 11. 参照資料

@/Users/2001y/.codex/RTK.md
